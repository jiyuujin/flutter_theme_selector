name: flutter_theme_selector
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  flutter_riverpod: ^1.0.3

dev_dependencies:
  build_runner:
  flutter_test:
    sdk: flutter
  pedantic_mono: ^1.17.0

flutter:
  uses-material-design: true
